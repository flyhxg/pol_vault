# HMM 隐马尔可夫模型 - AI 量化交易系统

> 核心思路：先判断市场环境，再决定交易动作

---
https://woshixiaobi.com/hmm-claude-ai/
## 一、核心概念

### 什么是 HMM？
- **英文**：Hidden Markov Model
- **简称**：HMM
- **理解**：一个"市场状态识别器"

### 工作原理
- 根据历史行情数据，把市场划分成不同状态：
  - 强势多头
  - 弱势空头
  - 来回震荡
  - 趋势衰减
- 演示版本把市场分成 **7 个状态**
- 模型告诉你现在更适合积极参与，还是先保守观望

### 与传统 TradingView 脚本的区别

| 传统脚本 | HMM 模型 |
|---------|---------|
| 线性固定规则（RSI > 70 就卖，均线金叉就买） | 带概率判断能力 |
| 市场结构变化后策略很快失效 | 先判断大环境，再决定是否用策略 |
| 死盯着某一个条件 | 判断"市场环境"后再决定交易动作 |

---

## 二、系统架构

### 两层职责分离

```
┌─────────────────────────────────────┐
│  第一层：市场状态识别器（HMM）        │
│  - 负责判断大环境                    │
│  - 判断现在是什么市场环境             │
│  - 相对稳定                          │
└─────────────────────────────────────┘
                 ↓
┌─────────────────────────────────────┐
│  第二层：执行层                       │
│  - RSI、MACD、ADX、成交量             │
│  - 风控、冷却时间                     │
│  - 决定什么时候出手/不出手/平仓        │
└─────────────────────────────────────┘
```

**核心观点**：只有两层配合起来，整个 AI 交易系统才算真正成型

---

## 三、提示词 1 - Google Colab HMM 市场状态识别

### 提示词
```
写一个用于 Google Colab 的 Python 脚本，使用隐马尔可夫模型（HMM）对比特币进行市场状态识别（Market Regime Detection）。
```

### 要求 1：安装库与获取数据

**安装库：**
```bash
pip install yfinance hmmlearn matplotlib pandas numpy scikit-learn
```

**获取数据：**
- 使用 `yfinance` 下载 BTC-USD 最近 730 天的小时级数据
- 必须使用 `yf.download`，设置 `period="730d"` 和 `interval="1h"`
- 处理 MultiIndex 多级列，最终列名必须为：`['Open', 'High', 'Low', 'Close', 'Volume']`
- MultiIndex 展开时使用 `get_level_values(0)`
- 确保 KeyError 问题被正确处理

### 要求 2：特征工程

**创建 3 个特征：**
```python
Returns = Close.pct_change()
Range = (High - Low) / Close
Vol_Change = Volume.pct_change()
```

**数据清洗：**
1. 删除 NaN
2. 替换 inf/-inf 为 NaN
3. 再次删除 NaN

### 要求 3：训练模型

**初始化 HMM：**
```python
from hmmlearn.hmm import GaussianHMM

model = GaussianHMM(
    n_components=7,
    covariance_type="full",
    n_iter=1000,
    random_state=42
)
```

**拟合模型：**
```python
X = data[['Returns', 'Range', 'Vol_Change']].values
model.fit(X)
data['State'] = model.predict(X)
```

### 要求 4：分析

**打印汇总表（每个状态占一行）：**
- `Mean_Return`：Returns 的均值
- `Volatility`：Returns 的标准差
- `Count`：样本数量
- 按 `Mean_Return` 从高到低排序

### 要求 5：可视化

- 绘制比特币最近 500 小时的收盘价
- 叠加散点图，用不同颜色表示检测到的状态（7 个 regime）
- 使用明显区分的 colormap，并包含图例

### 输出要求
- 返回一份可直接运行的 Colab 脚本（只要代码）

---

## 四、提示词 2 - Streamlit 交易应用

### 提示词
```
使用 Python、Streamlit、Plotly 和 yfinance，构建一个专业的基于市场状态（Regime-Based）的交易应用。
```

### 1. 核心引擎（HMM 逻辑）

- 使用 `hmmlearn.GaussianHMM`，设置 7 个状态组件
- 基于 3 个特征训练：
  - `Returns`（收益率）
  - `Range`（波动区间，`High-Low/Close`）
  - `Volume Volatility`（成交量波动）

**关键要求：**
- 自动识别 **"Bull Run（牛市强势状态）"**——正收益最高的状态
- 自动识别 **"Bear/Crash（熊市/暴跌状态）"**——收益最低的状态

### 2. 策略逻辑

**8 个确认条件的投票系统：**

| 条件 | 说明 |
|-----|-----|
| RSI < 90 | 相对强弱指标 |
| Momentum > 1% | 动量 |
| Volatility < 6% | 波动率 |
| Volume > 20周期 SMA | 成交量 |
| ADX > 25 | 平均趋向指数 |
| Price > EMA 50 | 价格位置 |
| Price > EMA 200 | 价格位置 |
| MACD > Signal Line | MACD 信号 |

**进场条件：**
- HMM 判断当前市场状态为看涨
- 8 个条件中至少满足 7 个

### 3. 风险管理规则

**冷却时间：**
- 发生平仓后，强制进入 **48 小时**冷却期
- 冷却期内不能再次进场
- 目的：避免震荡行情里反复打脸

**出场规则：**
- 如果市场状态切换成 "Bear" 或 "Crash"，立即平仓

**杠杆：**
- 在盈亏计算中模拟 **2.5 倍**杠杆

### 4. 系统架构

```
├── data_loader.py    # 使用 yfinance 获取 BTC-USD 最近 730 天的小时级数据
├── backtester.py     # 用 1 万美元初始资金运行回测模拟，记录每一笔交易日志
└── app.py            # Streamlit 可视化仪表盘
```

### 5. 仪表盘设计

**顶部：**
- 显示当前信号（Long 做多 / Cash 空仓）
- 显示识别到的市场状态

**图表部分：**
- 使用 Plotly 绘制交互式 K 线图
- 根据市场状态动态切换背景颜色：
  - 牛市状态 → 绿色背景
  - 熊市状态 → 红色背景

**指标区：**
- Total Return（总收益）
- Alpha vs Buy & Hold（相对买入并持有的超额收益）
- Win Rate（胜率）
- Max Drawdown（最大回撤）

---

## 五、提示词 3 - AGGRESSIVE Mode（激进模式）

### 提示词
```
这个策略运行得很不错，但我想测试一个'高风险'版本。
任务：修改 backtester.py，新建一个叫做'AGGRESSIVE Mode（激进模式）'的新模式。
```

### 修改内容

| 参数 | 原版 | 激进版 |
|-----|-----|-------|
| 杠杆 | 2.5 倍 | **4 倍** |
| 进场条件 | 7/8 | **5/8** |
| 追踪止损 | 无 | **5%** |

**UI 控制：**
- Streamlit 侧边栏添加复选框
- 启用/关闭"AGGRESSIVE Mode"
- 切换后重新运行回测

---

## 六、实际操作流程

```
1. 用 ChatGPT 生成 HMM 的 Python 草稿代码
         ↓
2. 把代码放到 Google Colab 里先跑通
   （好处：不用在电脑上配置复杂环境，浏览器里直接运行 Python）
         ↓
3. 底层模型没问题后，迁移到 Visual Studio Code
         ↓
4. 结合 Claude Code 继续扩展成完整的交易应用
```

---

## 七、持续优化过程

### 核心观点
- HMM 市场状态识别模型跑通后，不是"写完就结束"
- 进入可以**不断优化、不断进化**的过程

### 在 Claude Code 里可以做的
- 修改参数
- 调整条件
- 增加过滤器
- 优化风控
- 反复测试，找到适合自己交易风格的系统

### AI 编程工具的优势
- 过去不懂代码几乎无法做这些修改
- 现在只需把想法说清楚，AI 就会：
  - 改文件
  - 调逻辑
  - 补代码
- 不再是一个人碰复杂代码，而是和 AI 开发助手一起做系统

---

## 八、过拟合警告

### 常见误区
- 回测时只盯着历史收益曲线漂不漂亮
- 看到过去两年收益很高，就觉得策略已经稳了

### 过拟合风险
- 不断调参数、贴合过去数据
- 策略非常适合过去，却未必适合未来

### 合理做法
1. 先把底层逻辑搭出来
2. 用一段时间观察、测试、甚至跑模拟盘
3. 再慢慢回头优化

---

## 九、扩展控制逻辑

### 冷却时间
- 设置 48 小时冷却时间
- 一笔仓位刚平掉后，机器人在接下来 48 小时内不允许再次进场
- 目的：避免连续追单

### 可继续叠加的条件
- 更严格的 RSI 条件
- 更多趋势过滤器
- 不同时间周期的共振确认

### 目的
- 不是让系统变得越来越复杂
- 而是让它在真实市场里更稳一点、少犯错一点

---

## 十、交易所与工具推荐

### 加密货币交易所

| 交易所 | 邀请码 | 手续费减免 |
|-------|-------|----------|
| Binance 币安 | BFCYEW71 | 20% |
| OKX 欧易 | 88576911 | 20% |
| Bybit | 95292 | 20% |
| Bitget | - | - |
| Kraken 海妖 | - | 出金合规渠道 |

### 链上交易
- GMGN

### 自动化工具

| 工具 | 用途 |
|-----|-----|
| SignalStack | 自动化信号传送 |
| 3Commas | 自动化交易机器人 |
| Bookmap | 主力挂单、二级行情、热力图 |
| ChartPrime | 辅助提高策略确认率 |

### 美股/期权/期货
- IB 盈透证券
- OptionStrat
- NinjaTrader

### AI 工具
- Claude Code：读代码、改文件、跑命令、修复错误
- ChatGPT Plus：生成代码草稿
- 银河录像局：ChatGPT Plus 代充（国内用户）

---

## 十一、核心价值总结

### 不是什么
- 不是一上来就找到稳赚不赔的圣杯策略

### 而是什么
具备更接近专业量化交易员的思路：

```
先判断环境，再决定出手
先搭模型，再叠加策略
先跑通逻辑，再持续优化
```

### 最终产出
- 不再是一个简单指标
- 而是一套能随着市场变化**不断调整、不断训练、不断升级**的 AI 交易系统

---

## 标签

#量化交易 #HMM #隐马尔可夫模型 #AI交易 #市场状态识别 #Claude #Python #Streamlit #比特币
